<Window x:Class="FocusTime.App.Views.SessionRecoveryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Session Recovery"
        Width="450"
        Height="250"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Session Recovery"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1"
                   Text="A previous session was interrupted. Would you like to continue?"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="{Binding SessionInfo}" TextWrapping="Wrap" Opacity="0.8"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Discard"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"
                    Background="#CC0000"
                    Foreground="White"
                    BorderThickness="0"
                    Click="Discard_Click"
                    Cursor="Hand"/>
            <Button Content="Resume"
                    Width="100"
                    Height="35"
                    Background="#0078D4"
                    Foreground="White"
                    BorderThickness="0"
                    Click="Resume_Click"
                    Cursor="Hand"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
