Bạn là Senior C# WPF Engineer. Bạn đang làm việc trên repo FocusTime (C# .NET 8, WPF, WebView2, local JSON). 
Mục tiêu: cải tiến chất lượng theo danh sách issue đã cung cấp (thread-safety, error handling, session recovery, distraction prompt, IDisposable, persist domain timeouts, minimize behavior, settings validation, longest streak, timer precision).

Ràng buộc:
- Không thêm backend/cloud/login/telemetry.
- Không thay đổi feature scope ngoài các mục cải tiến.
- Giữ kiến trúc Core/App tách bạch; ưu tiên sửa tối thiểu nhưng đúng.
- Mọi dữ liệu vẫn local-only (JSON).
- Nếu có notification: giữ toast nếu đã có, luôn có fallback popup + sound.

Cách làm:
1) Trước khi sửa: tóm tắt nhanh kiến trúc hiện tại và điểm chạm của từng issue.
2) Với mỗi issue: đề xuất phương án + file sẽ sửa.
3) Sau đó mới sửa code.
4) Trả về: danh sách file thay đổi + giải thích + snippet/diff theo file.
5) Kết thúc: checklist manual test (5–10 bước) và lệnh build/test.

BẮT ĐẦU với “Quick Wins” trước (thread-safety + try/catch + validation + persist timeouts + longest streak), sau đó mới tới các feature nặng hơn.